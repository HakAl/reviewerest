{
  "id": "r57",
  "request": "Review this password-reset implementation before merge. The token lookup and store below are the entire relevant implementation. Return the structured review record.",
  "mode": "noninteractive",
  "capabilities": {
    "source_inspection": true,
    "artifact_execution": false,
    "isolated_host_checks": false,
    "editing": false,
    "specialist_references": true
  },
  "context": {},
  "artifacts": [
    {
      "source": "reset.py",
      "revision": "fixture-v1",
      "text": "def reset_password(token, new_password, now, store):\n    row = store.tokens.get(token)\n    if row is None or row.expires_at <= now:\n        return False\n    store.set_password(row.account_id, new_password)\n    return True\n"
    }
  ]
}
